version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install --upgrade pip
      - pip install --upgrade setuptools wheel
  build:
    commands:
      - pip install -r requirements.txt

artifacts:
  files:
    - '**/*'
  discard-paths: no